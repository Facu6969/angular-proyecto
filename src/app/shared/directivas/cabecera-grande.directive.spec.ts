import { CabeceraGrandeDirective } from './cabecera-grande.directive';

describe('CabeceraGrandeDirective', () => {
  it('should create an instance', () => {
    const directive = new CabeceraGrandeDirective();
    expect(directive).toBeTruthy();
  });
});
